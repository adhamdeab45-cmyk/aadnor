{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "admins": {
      ".read": "auth != null && (root.child('admins').child(auth.uid).val() === true || auth.token.email === 'adhamdeab2@gmail.com')",
      ".write": "auth != null && (root.child('admins').child(auth.uid).val() === true || auth.token.email === 'adhamdeab2@gmail.com')"
    }
  }
}
