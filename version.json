{
  "version": "2.1.0",
  "build": "V210-WHEEL-AUDIO-SYNC",
  "changes": [
    "wheel sound now starts only when the wheel visually begins rotating",
    "tick rhythm slows naturally with wheel deceleration",
    "result sound waits for the real CSS transition end instead of a fixed timer",
    "removed duplicate legacy wheel sound handler"
  ]
}
