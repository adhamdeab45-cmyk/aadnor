{
  "version": "2.1.5",
  "build": "V215-GAME-ACCOUNT-LINK-WHEEL-FIX",
  "changes": [
    "repairs the secure wheel USER_NOT_FOUND failure by resolving the authenticated UID to the financial user record",
    "adds an authenticated game-account preparation endpoint and one safe retry with the same request id",
    "keeps idempotency so a retry cannot charge the wheel twice",
    "updates the wheel status text after server errors instead of leaving it stuck on preparing",
    "keeps Firebase indexes and DOM cleanup from V214"
  ]
}
