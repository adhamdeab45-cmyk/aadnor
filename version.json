{
  "version": "2.1.8",
  "build": "V218-AUTHORITATIVE-DRAW-SCHEDULER",
  "changes": [
    "routes every admin draw result action through the authenticated Node server",
    "stores scheduled winner data only in private server-only Firebase nodes",
    "publishes daily, weekly, monthly and yearly results automatically at the saved Turkey-time timestamp",
    "credits the prize exactly once and settles winning, losing and moved tickets only when publication time arrives",
    "keeps the previously published result and top gold ticker visible while a future result is waiting",
    "adds exact next-result timer, 15-second fallback scheduler and public self-healing result poll"
  ]
}
